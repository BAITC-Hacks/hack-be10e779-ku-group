# TASKS
Статусы: [ ] не начато · [~] в работе · [x] готово и проверено

## Час 1 (13:00–14:00)
- [x] (Максим) Разбор кейса → `CASE.md`, данные турбин в `data/raw/` — проверка: файлы в `main`
- [~] (Максим) Ядро по ТЗ: загрузка данных → архивная погода Open-Meteo → модель → бэктест февраля → CSV — проверка: `python -m app.forecast.cli backtest` создаёт `outputs/forecast_feb2026.csv`
- [~] (Оксана) План веба: экраны, API-контракт `docs/api-contract.md` — проверка: контракт утверждён Максимом
- [~] (Вова) Фичи сверх ТЗ под критерии «ценность 15» и «оригинальность 10»: список с оценкой «время/польза» — проверка: список в `DECISIONS.md`

## Час 2 (14:00–15:00)
- [ ] (Максим) Агент: инструменты цикла R4 (погода → подготовка → модель → прогноз → анализ → пересчёт), планировщик без ключа, режим LLM с ключом
- [ ] (Максим) API: `/api/forecast`, `/api/backtest`, `/api/metrics`, `/api/history`
- [ ] (Оксана) Экран прогноза: выбор даты, график прогноз/интервал, журнал шагов агента (на моках по контракту)
- [ ] (Вова) Первая выбранная фича

## Час 3 (15:00–16:00)
- [ ] Сквозной проход UI → API → агент → прогноз; метрики января в README
- [ ] `/smoke` на чистом клоне

## NOT NOW
- P1: ансамбль моделей погоды, квантильная калибровка, деплой на hack.aican.cloud
- P2: только в README — N турбин, внутридневная коррекция, балансирующий рынок
