import type ru from '../ru/common'
import type { Dict } from '../../core'

const en: Dict<typeof ru> = {
  retry: 'Retry',
  close: 'Close',
  tabError: 'Couldn’t display the “{name}” tab: {error}. Other tabs still work.',
}
export default en
