# Память бэкенда (Максим)

## Данные
- 13:35 · находка · `data/raw/turbine{1,2}.csv`: шаг 10 мин, 11.03.2023–31.01.2026, мощность 0–1, пропусков значений нет; у Т1 дыра май–июль 2024. Факта за февраль 2026 НЕТ.
- 13:36 · находка · время в данных — местное UTC+5: летом максимум температуры в 15:00, минимум в 06:00.
- 13:50 · находка · часов «остановки» (мощность <0.02 при ветре >6 м/с) всего 15 — фильтр простоев ничего не даёт.

## Погода
- 13:40 · решение · источник — Open-Meteo Previous Runs API, только колонки `*_previous_day1/2` (выпуски за 24/48 ч до часа). Прогноз делается в конце дня D: D+1 ← day1, D+2 ← day2 → все выпуски доступны на момент прогноза. Фактическую погоду не используем нигде.
- 13:45 · находка · архив day1/day2 для точки станции есть с 16.02.2024 (best_match). Обучение — на 2024-02…2026-01.
- 13:48 · находка · ветер 100 м в архиве есть у `ecmwf_ifs025`, `icon_global`, `gfs_seamless`; у `jma_seamless`, `cma_grapes_global`, `gem_seamless` — только 10 м; у meteofrance/ukmo/aifs — пусто или не с 2024. Все ответы сохранены в `data/weather/` (работа без интернета).
- · тупик · исторический ensemble-API (члены ансамбля) для прошлых дат недоступен.

## Модель
- 13:40 · решение · scikit-learn HistGradientBoosting, квантили p10/p50/p90 + отдельные модели по турбинам; без LightGBM (лёгкий Docker).
- 13:52 · находка · ансамбль 6 моделей погоды (значения + среднее + разброс) снижает MAE на 4 месяцах (окт–янв, скользящее окно) с 0.169 до 0.151 → принято.
- 13:53 · результат · скользящее окно окт 2025–янв 2026: MAE D+1 0.145 (кривая мощности 0.178, персистентность 0.356); D+2 0.157 (0.191 / 0.373). Покрытие p10–p90 0.69 при цели 0.80 — надо калибровать.
- · todo · конформная калибровка интервала по бинам ветра; pinball/CRPS; плотность воздуха (surface_pressure); вес свежих данных.

## Цикл ТЗ (pipeline.py)
- 13:45 · решение · шаги: get_weather → prepare → run_model → hourly_forecast → analyze (флаги) → compare_with_previous (пересчёт: сутки D+1 вчера прогнозировались по выпуску day2, сегодня — по day1; показываем изменение).
- 13:45 · результат · `python -m app.forecast.cli backtest` → `outputs/forecast_feb2026.csv` (28 выпусков × 48 ч), `outputs/forecast_feb2026_final.csv` (каждый час февраля — свежайший прогноз D+1), ~30 с.

## Агент (agent.py, calibrate.py)
- 14:05 · решение · агент = 6 инструментов (rank_weather_sources, fetch_weather с проверкой честности по времени, build_features, run_forecast с калибровкой, analyze_forecast, compare_with_previous). LIVE — порядок и исключение источников решает LLM (gpt-4o-mini, function calling); DEMO — те же правила в коде. Числа считает только код.
- 14:05 · находка · конформная калибровка по режимам ветра (окт–дек → проверка январь): покрытие p10–p90 0.634 → 0.745, ширина 0.435 → 0.464. До 0.80 не дотянули — честно пишем в README.
- 14:05 · находка · точность источников ветра за 30 дней до 31.01 (MAE против ветра на турбинах): ECMWF 10 м 1.84, best_match 100 м 1.95, … GFS 100 м 3.30 (худший).
- 14:06 · находка · LIVE-прогон на 2026-02-09 прошёл весь цикл; LLM один раз вызвала analyze раньше run_forecast — инструмент вернул «сначала run_forecast», агент исправился. Первый вызов модели ~50 с (обучение), дальше из памяти.

## API (app/api/forecast.py)
- 14:06 · решение · API · эндпоинты по контракту: POST /api/forecast, POST /api/backtest, GET /api/metrics, GET /api/history; ValueError → 400, httpx.HTTPError (нет погоды и кэша) → 503, прочее → 500 без деталей. Замена цикла на агента — одна строка в `run_cycle()`. Тесты: `tests/test_api_forecast.py` (21, зелёные).
- 14:06 · решение · API · `/api/backtest` перезаписывает `outputs/forecast_feb2026.csv` только при полном периоде 31.01–27.02 (через `cli.backtest`); частичный диапазон считается в памяти, `file: null` — **отличие от контракта** (там file всегда строка), нужно согласовать с Оксаной.
- 14:06 · решение · API · в `HourPoint` добавлено необязательное поле `curve` (базовая кривая мощности, её отдаёт pipeline); в `AgentStep` добавлен шаг `hourly_forecast` (есть в цикле ТЗ, нет в списке контракта). `/api/history`: не больше 93 дней, 400 вне диапазона данных, часы с неполными данными → null.
- 14:06 · находка · API · первый вызов `full_cycle` ~70 с (обучение модели), дальше ~0.3 с. Первый запрос с фронта будет висеть минуту — предлагаю прогрев модели в lifespan при старте (владелец main.py/forecast решает).
- 14:06 · находка · API · `/api/backtest` на полный период вызывает цикл дважды (для флагов и внутри `cli.backtest`), ~20 с после прогрева. Лечится, если `cli.backtest` будет возвращать результаты full_cycle.
- 14:06 · находка · API · ms всех шагов = 0: `full_cycle` не отдаёт тайминги; появятся, когда цикл пойдёт через агента.
- 14:06 · находка · погода · **утечка в Previous Runs**: значение `previous_day1` в часе T взято из запуска ≈ T−24 ч (проверено по Single Runs ECMWF, совпадение до сотых). При t0 = D 23:59 UTC+5 (18:59 UTC) час D+1 23:00 (18:00 UTC) взят из запуска 18 UTC дня D, который выходит ≈ 00:27 UTC D+1 — после t0. То же для последнего часа D+2 по day2. Итого ≈ 2 ч из 48 в каждом выпуске (у GFS/ICON задержки другие — проверить). Честная альтернатива — Open-Meteo Single Runs API (`run=`), ECMWF IFS с 14.03.2024.
- 14:29 · решение · API · `/api/forecast` → `agent.run()` (LIVE/DEMO, шаги с ms, time_integrity, excluded_sources). `/api/backtest` оставлен на `pipeline.full_cycle` — тот же цикл, которым `cli.backtest` пишет CSV; агент с LLM на 28 выпусков в HTTP-запросе — минуты и деньги. **Итог: прогноз в UI (агент: калибровка интервала, исключение источников) и CSV февраля (pipeline) считаются разными путями** — решить владельцу forecast/.
- 14:29 · решение · API · прогрев: lifespan запускает фоновый поток `warm_up()` (get_forecaster + calibrate.load); `/health` → `model: cold|warming|ready|error`, `status` всегда ok (healthcheck Docker не падает во время обучения). Запрос во время прогрева ждёт его конца (до 300 с), модель второй раз не обучается.
- 14:29 · находка · Docker · в образ не копировался `outputs/` → нет metrics.json и calibration.json, `calibrate.load()` пытался бы писать в /app/outputs (root) → добавлено `COPY outputs/` и chown.
- 14:29 · результат · Docker · чистый клон (HEAD + diff API), `cp .env.example .env`, без ключа: сборка 131 с, /health через 13 с (warming), модель ready через 138 с от старта, первый POST /api/forecast 2026-02-09 — 200 за 5.1 с (run_forecast 4.3 с на первом вызове, дальше доли секунды). `scripts/smoke.sh` пройден. Замеры при load average ~26 (параллельно шли тесты других сессий) — на свободной машине быстрее.
- 14:29 · находка · тесты · обучение HistGradientBoosting берёт все ядра через OpenMP; несколько pytest/обучений одновременно на одной машине тормозят друг друга в разы (тест с настоящим агентом шёл >13 мин при load ~29). Совет: не гонять полные тесты параллельно из нескольких сессий или ставить OMP_NUM_THREADS.
- 14:41 · находка · тесты · `test_forecast_real_agent` (настоящий агент, обучение модели) на рабочей копии forecast/ шёл 435 с при load ~22; остальные 22 теста API — 28 с. Быстрый прогон: `pytest tests/test_api_forecast.py -k 'not real_agent'`.

## Исправление утечки погоды (14:20–14:45)
- 14:20 · находка · (нашла сессия API) выпуск NWP публикуется через ~4–7 ч после срока; правило «D+1 ← day1, D+2 ← day2» брало для поздних часов выпуск, вышедший после 23:59 дня D.
- 14:25 · решение · `model.run_lead`: для каждого часа — минимальный N∈{1,2,3}, при котором (час − N сут) + 7 ч ≤ момент прогноза. На выпуск 09.02: day1 — 17 ч, day2 — 24 ч, day3 — 7 ч. Архив перекачан с `previous_day3`. Тест `test_every_weather_run_used_was_published_before_issue_time`.
- 14:30 · результат · честные метрики (окт–янв): MAE D+1 0.148 (кривая 0.182, персистентность 0.356), D+2 0.160 (0.193 / 0.373) — было 0.145/0.157 с утечкой.
- 14:44 · результат · калибровка без утечки: покрытие p10–p90 на январе 0.658 → 0.764.
- 14:40 · решение · nMAE = MAE в долях номинала (отраслевой стандарт; мощность уже нормирована). Ранее считали MAE/среднее (0.41) — это поле теперь `mae_to_mean`.
- · todo · `cli.backtest` идёт через pipeline без калибровки и без исключения источников → перевести на детерминированный планировщик агента, чтобы CSV = UI.

## Эксперимент признаков (Codex)

- 14:52 · методика · `backend/experiments/features_exp.py`: скользящее окно 2025-10…2026-01, для каждого месяца обучение только до его начала; текущие честные `model.LEADS`, `horizon_leads()` и параметры p50-модели. Плюс в колонках месяцев означает снижение среднего MAE двух горизонтов относительно базы. Критерий «брать»: снижение не меньше 0.002 минимум в 3 из 4 месяцев.
- 14:52 · данные · Open-Meteo Previous Runs для `best_match`, `ecmwf_ifs025`, `icon_global`: `surface_pressure`, `wind_speed_80m`, `wind_speed_120m`, `relative_humidity_2m`, выпуски day1/day2/day3; сохранены отдельно в `data/weather/exp_*.json`. Влажность сохранена по заданию, но не использована: формула кандидата задана как `rho = p / (287.05 * T[K])`.

| Вариант | MAE D+1 | MAE D+2 | Окт | Ноя | Дек | Янв | Месяцев ≥0.002 | Решение |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| База (`FEATURES`) | 0.1475 | 0.1596 | +0.0000 | +0.0000 | +0.0000 | +0.0000 | — | контроль |
| Плотность `rho` | 0.1481 | 0.1594 | +0.0014 | −0.0004 | −0.0024 | +0.0005 | 0 | **не брать** |
| `rho * ws100³` | 0.1469 | 0.1586 | +0.0008 | +0.0001 | +0.0006 | +0.0016 | 0 | **не брать** |
| Ветер 80/120 м, 3 источника | 0.1485 | 0.1600 | +0.0006 | +0.0009 | −0.0020 | −0.0024 | 0 | **не брать** |
| Вес свежести `0.5 ** (возраст / 180)` | 0.1458 | 0.1591 | +0.0037 | +0.0018 | +0.0021 | −0.0033 | 2 | **не брать** |
| Все кандидаты вместе + вес | 0.1466 | 0.1602 | −0.0007 | +0.0016 | +0.0011 | −0.0013 | 0 | **не брать** |

- 14:52 · результат · ни один кандидат не прошёл заранее заданный порог. Вес свежести — единственный близкий вариант, но улучшил MAE ≥0.002 только в октябре и декабре и ухудшил январь на 0.0033; в `model.py` не переносить.
- 14:51 · решение · API · v2: `GET /api/meta`, `GET /api/forecast/{date}`, `GET /api/backtest`, `GET /api/backtest/csv?kind=all|final`, `GET /api/holdout?issue_date=`; `HourPoint.weather_run_days` (1|2|3). 39 тестов `-k 'not real_agent'` зелёные.
- 14:51 · решение · API · `GET /api/forecast/{date}`: сначала кэш последних 64 прогнозов агента **в памяти процесса** (наполняет POST), иначе восстановление из `outputs/forecast_feb2026.csv` — без журнала шагов и флагов, `origin: "backtest_csv"`, `steps: []`, `computed_at` = время файла. Отличие от контракта v2: там `outputs/runs/{date}.json` на диске; кэш в памяти теряется при перезапуске. **Другая сессия создала `outputs/forecasts/`** — если это сохранённые прогнозы агента, GET стоит читать оттуда (одна строка в `saved_forecast`).
- 14:51 · решение · API · новые поля `Forecast.origin` (agent | backtest_csv) и `computed_at` — в контракте есть только computed_at, origin добавить в v2. `meta.saved_forecasts` = кэш ∪ даты из CSV (все 28).
- 14:51 · решение · API · `/api/history` — лимит 62 дня (как в v2, было 93).
- 14:51 · результат · API · `outputs/holdout_jan2026.csv` (1 488 строк = 31 день × 24 ч × 2 горизонта, факт во всех: часы без погоды или факта `_month_predictions` отбрасывает): модель, обученная до 31.12.2025, интервал с той же калибровкой, что у агента. Январь: MAE модели 0.158, кривой мощности 0.168, покрытие p10–p90 0.767. Считается 37 с; если файла нет — эндпоинт строит его один раз под блокировкой. Источник — `calibrate._month_predictions("2026-01")` (приватная функция, как в контракте).
- 14:51 · находка · API · на отдельных днях модель хуже кривой (выпуск 2026-01-20: MAE 0.207 против 0.199) — выигрыш только в среднем за месяц; в UI не обещать «лучше каждый день».
- 14:51 · находка · API · path traversal в `/api/forecast/{date}`: `..%2F..` не доходит до роутера (JSON 404 «Не найдено»), мусор без «/» отсекает регулярка YYYY-MM-DD (400); путь к файлу из даты не строится вовсе. `/api/backtest/csv?kind=` — только all|final (белый список).
- 14:51 · todo · API · не сделано из v2: `POST /api/backtest` без тела с ответом `Backtest` и 409 при параллельном прогоне; `Metrics.coverage/calibration` отдельными полями; `fallback`; jobs (P1).

## Паспорт и версии (E1 из спеки Вовы), карточки (E3) — 15:05
- решение · `passport.py`: каждый выпуск → `outputs/forecasts/<issue>_<hash8>.json` (паспорт + прогноз). Паспорт: decision_at/target (UTC+5), weather_profile (выпуски по суткам, задержка 7 ч), as_of_status, effective/excluded sources, model_id, calibration_id, input_hash, result_hash, execution_mode. Версия не перезаписывается.
- решение · `compare_with_previous` теперь сравнивает с СОХРАНЁННЫМ прошлым выпуском (D+2 вчера vs D+1 сегодня, 24 общих часа, часы с изменением ≥10 п.п.). Реконструкция — только если прошлого выпуска нет (31.01), с пометкой method.
- решение · `cli.backtest` идёт через `agent.run(planner_only=True)` — CSV февраля = то, что показывает UI (калибровка, выбор источников, паспорт).
- результат · повторный прогон февраля дал те же 28 forecast_id — воспроизводимость по хешу входов подтверждена.
- находка · планировщик на всех выпусках исключает `gfs_ws100` (MAE 3.3 м/с > 1.3×медианы) — штатное решение, в карточки «ненадёжный вход» не выносится.
- фикс · кривая мощности больше не подставляет 0 при пропуске ветра.
- 15:02 · решение · API · под фронт (frontend/src/api/endpoints.ts + types.ts): `GET /api/forecast/{date}` читает **`passport.latest`** (outputs/forecasts) + паспорт, `computed_at` = passport.created_at; 404 если выпуска нет. Кэш в памяти и восстановление из CSV убраны, поле `origin` убрано. `GET /api/backtest` — 28 последних версий из outputs/forecasts **с флагами** (раньше из CSV, flags = []). `POST /api/backtest` — тело `{}`, только весь период (частичный → 400), параллельный → 409, ответ = тот же `Backtest`. `meta.saved_forecasts` — даты из outputs/forecasts. В `Forecast` добавлены `cards` (типизированы), `passport` (dict), `fallback`.
- 15:02 · результат · API · uvicorn на чистом клоне + curl по каждому вызову endpoints.ts: /health, /api/meta, GET и POST /api/forecast, GET и POST /api/backtest, /api/backtest/csv?kind=all|final, /api/metrics, /api/history, /api/holdout — все 200, поля совпадают с types.ts. POST /api/forecast на 2026-02-09 вернул тот же forecast_id 2026-02-09_409e1527, что в хранилище (воспроизводимость). pytest `-k 'not real_agent'` — 60 зелёных.
- 15:02 · находка · API · расхождения с types.ts, которые не в зоне API: (1) `Health.llm_model` фронт ждёт в /health — main.py его не отдаёт (фронт берёт из /api/meta, не ломается); (2) `time_integrity.issued_at` = "2026-02-09 23:59:00", контракт хочет "2026-02-09T23:59" — формат в agent.py; (3) `fallback` агент не заполняет → приходит null, фронт вычисляет сам по журналу; (4) `computed_at` в UTC (+00:00), остальные времена — UTC+5 без смещения; (5) `Metrics.coverage/calibration` не отдаём (в types.ts необязательные).
- 15:02 · находка · API · `GET /api/backtest` берёт **последнюю** версию каждой даты: если диспетчер сделает LIVE-прогноз на дату февраля, календарь покажет его, а CSV — прогон планировщика. Для сдачи CSV — эталон; при расхождении смотреть forecast_id в паспорте.
- 15:02 · находка · API · `GET /api/forecast/{date}` вне 31.01–27.02 → 400, а `endpoints.ts` превращает в null только 404/405 — вне диапазона фронт получит ошибку (сейчас календарь вызывает только даты диапазона).

## Честность оценки (по разбору Вовы) — 15:25
- фикс · граница обучения в скользящей проверке и калибровке: S − 1 сут − 1 ч (раньше S − 1 ч давала D+2 на 1-е число сутки «будущего» факта).
- фикс · конформная поправка — точная k-я порядковая статистика (k = ceil((n+1)·0.8)), без интерполяции.
- результат · окт–янв: nMAE D+1 14,9 % (кривая 18,2 %, персистентность 35,6 %), D+2 16,1 % (19,3 % / 37,3 %). Калибровка на январе: покрытие 0,665 → 0,775, ширина 0,445 → 0,477.
- решение · B0/B1: исключение gfs_ws100 по ошибке ветра НЕ улучшает (D+1 0,1495 vs 0,1490) → агент исключает источник только при пропусках в окне. Метрика в README = то, что делает агент.
- для питча · не говорить «первый ИИ-прогноз в РК» — у Самрук-Энерго (20.08.2026) и KEGOC уже есть прототипы; наш аргумент — честная проверка по времени, паспорт выпуска, измеренная польза.
